<% content_for :title do %>Avatar Sizes on <% end %>

<% content_for :breadcrumbs do %>
	<div id="breadcrumbs">
		<%= render :partial => "shared/breadcrumbs/about" %>
		<div class="cell child"><span class="l"></span><span class="c">Avatars</span><span class="r"></span></div>
	</div>
<% end %>
		<div id="copy">
		
			<div class="column width960">

				<div class="column width700">

					<div class="module width700">

<h1>Icons: Measurements of Symbolic Weight</h1>
<h5>ahah!</h5>
<br /><br />

<% if logged_in? %>
mini: <%= avatar_link_to_user :user => current_user, :size => 'mini' %>
<br clear="all" />

toolbar: <%= avatar_link_to_user :user => current_user, :size => 'toolbar' %>
<br clear="all" />

tiny: <%= avatar_link_to_user :user => current_user, :size => 'tiny' %>
<br clear="all" />

small: <%= avatar_link_to_user :user => current_user, :size => 'small' %>
<br clear="all" />

medium: <%= avatar_link_to_user :user => current_user, :size => 'medium' %>
<br clear="all" />

profile: <%= avatar_link_to_user :user => current_user, :size => 'large' %>
<br clear="all" />

large: <%= avatar_link_to_user :user => current_user, :size => 'large' %>
<br clear="all" />

<br />

<% end %>

						</div>
											
					</div>
				
				</div>

			
			</div>



		</div>
