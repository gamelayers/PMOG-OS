<% unless @messages.nil? || @messages.empty? %>
  <% for message in @messages %><% next if message.sender_id == current_user.id %>new Insertion.Bottom('stage', '<%= escape_javascript message_content(message) %>');hi<% end %>
  <% if @messages.length > 0 %>$('conversation').scrollTop = $('conversation').scrollHeight + $('conversation').clientHeight;<% end %>
<% end %>
